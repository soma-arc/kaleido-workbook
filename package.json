{
    "name": "hyperbolic-plane-tile-poc",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "typecheck": "tsc -p tsconfig.json",
        "lint": "biome check .",
        "format": "biome check --write .",
        "test": "vitest --run",
        "test:watch": "vitest",
        "test:sandbox": "vitest --run --pool=threads --poolOptions.threads.singleThread --no-file-parallelism",
        "coverage": "vitest run --coverage",
        "prepare": "husky",
        "ci": "pnpm biome:ci && pnpm typecheck && pnpm test:sandbox",
        "biome:ci": "biome ci ."
    },
    "devDependencies": {
        "@biomejs/biome": "^2.2.2",
        "@fast-check/vitest": "^0.2.2",
        "@types/react": "^18.3.5",
        "@types/react-dom": "^18.3.0",
        "@vitejs/plugin-react": "^4.3.1",
        "@vitest/coverage-v8": "^2.0.5",
        "fast-check": "^4.3.0",
        "husky": "^9.1.7",
        "jsdom": "^27.0.0",
        "lint-staged": "^16.1.6",
        "typescript": "^5.6.2",
        "vite": "^5.4.6",
        "vitest": "^2.0.5"
    },
    "dependencies": {
        "react": "^18.3.1",
        "react-dom": "^18.3.1"
    }
}
