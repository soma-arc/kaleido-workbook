import type { ReactNode } from "react";

/**
 * Multi-plane sceneでは既存の EuclideanSceneHost のコントロールを
 * 利用しているため、フェーズCでのホスト分割まで placeholder。
 */
export function EuclideanMultiPlaneControls(): ReactNode {
    return null;
}
